--!strict
-- Stub: we’ll implement melee/swords/guns in Phase 1.
local RS = game:GetService("ReplicatedStorage")
local Net = RS.Shared:WaitForChild("Shared"):WaitForChild("Net")
local Combat = Net.Combat

-- Example API shape to grow into:
-- Combat.OnHit, Combat.ApplyDamage, etc.
return {}
